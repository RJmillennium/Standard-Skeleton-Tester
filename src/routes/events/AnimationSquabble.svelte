<!-- <script>
  import { onMount } from "svelte";
  import lottie from "lottie-web";

  import animationData from "./Squabble_icon.json";

  let container;

  onMount(() => {
    const animation = lottie.loadAnimation({
      container,
      animationData,
    });

    return () => {
      animation.destroy();
    };
  });
</script>

<div class="max-h-20 max-w-20 animate-fade-in-once" bind:this={container} /> -->
